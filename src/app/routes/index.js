// all routes HTTP method (GET/PUT/POST/DELETE) will be present here

module.exports = (app) => {
    // import your existing user controller here, which contains method for all CRUD operations

    // APi endpoint to create a new user

    // API endpoint to get all user

    // API endpoint to get single user

    // API endpoint to update user with userId

    // API endpoint to delete user with userId

};